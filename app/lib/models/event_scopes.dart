/// Determines the time horizon for events to display.
enum EventTimeScope { future, past }

/// Determines the attendance filter for events to display.
enum EventAttendanceScope { all, confirmed, created }
