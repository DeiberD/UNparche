import 'package:flutter/material.dart';

/// Shared color tokens for the UNparche campus map theme.
const campusInk = Color(0xFF263020);
const campusBackground = Color(0xFFFBF5F2);
const campusSurface = Color(0xFFF3ECE8);
const campusAccent = Color(0xFFEEDDF0);
